[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  query = {fingerprint = ":fingerprint"}
  to = "https://:fingerprint--test-fingerprint.netlify.app/:splat?"
  status = 200
  force = true